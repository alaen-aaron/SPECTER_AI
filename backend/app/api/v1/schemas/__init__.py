"""
Pydantic v2 request/response schemas for the v1 API, split one module
per resource (auth, health, organizations, projects, targets,
authorization records) so each stays small and easy to find.
"""
